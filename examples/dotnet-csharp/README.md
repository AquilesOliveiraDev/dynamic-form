# Exemplo: C# / ASP.NET Core 8 com DynamicForm

Demonstração de integração com o ecossistema .NET:
- Minimal API tipada com `System.Text.Json`.
- Validação por atributos `[Required]`, `[Range]`, `[RegularExpression]`.
