# Exemplo: Go com DynamicForm (Server-Driven UI)

Demonstração de como construir um endpoint em Go que:
1. Emite o `DynamicFormSchema` padronizado para qualquer cliente frontend (Web, Mobile, Desktop).
2. Recebe e desserializa o payload validado.

### Como Executar
```bash
go run main.go models.go
```
